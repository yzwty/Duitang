<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.yzw.duitang">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/launchpad_icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.yzw.duitang.ui.MainActivity"
            android:label="@string/app_name">

        </activity>


        <activity
            android:name="com.yzw.duitang.ui.WelcomeActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.beginBackground">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name="com.yzw.duitang.ui.PersonActivity"
            android:screenOrientation="portrait"
            android:theme="@style/ActionBarOverlayTheme"
            android:parentActivityName="com.yzw.duitang.ui.MainActivity" >
            <!-- Parent activity meta-data to support API level 7+ -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.yzw.duitang.ui.MainActivity" />
        </activity>

    </application>

</manifest>
